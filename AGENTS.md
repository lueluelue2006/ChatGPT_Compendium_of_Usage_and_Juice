# AGENTS Notes

- Keep these four filenames fixed under `screenshots/`.
- When regenerating images, overwrite the same files so `README.md` automatically shows the latest images.

- 将这 4 个文件名固定在 `screenshots/` 目录下。
- 重新生成图片时，只需覆盖同名文件，`README.md` 会自动显示最新图片。
